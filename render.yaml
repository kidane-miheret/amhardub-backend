services:
  - type: web
    name: amhardub
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      GEMINI_API_KEY:
        fromDatabase:
          name: gemini-key
          property: value